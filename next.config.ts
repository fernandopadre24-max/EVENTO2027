
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Removido experimental.allowedDevOrigins que causava erros em versões novas do Next.js
};

export default nextConfig;
