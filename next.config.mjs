/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  experimental: {
    // Removido 'allowedDevOrigins' que não é mais suportado no Next.js 15
  },
};

export default nextConfig;
